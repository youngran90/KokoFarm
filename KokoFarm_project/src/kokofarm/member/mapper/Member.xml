<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="kokofarm.member.mapper.MemberMapper">
	<cache />

<insert id="insertMember" parameterType="Member">
	insert into Member  values(#{member_id}, #{member_password}, #{member_name}, #{member_email},
					 #{member_phoneNum},#{member_zipcode},#{member_address1}, #{member_address2},
					#{member_account}, '일반')			
					
</insert>

<select id="idcheck" parameterType="String" resultType="int">
	select count(*) from member where member_id = #{member_id}
</select>

<select id = "logincheck" parameterType="java.util.HashMap" resultType="int">
	select count(*) from member 
		where member_id=#{member_id} and member_password=#{member_password}
</select>

<select id = "selectMember" parameterType="String" resultType="Member">
	select * from member where member_id=#{member_id}
</select>


	</mapper>